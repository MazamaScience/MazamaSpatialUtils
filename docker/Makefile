################################################################################
# Makefile for building and running docker containers


# PRODUCTION version -----------------------------------------------------------
production_build:
	docker build --no-cache -t mazamascience/spatialutils:0.6.1 -t mazamascience/spatialutils:latest .

